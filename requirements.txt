# ─────────────────────────────────────────────────────────────
# Graph-Enhanced Real-Time Fraud Detection at 1M TPS
# Core runtime dependencies (required to run the pipeline)
# ─────────────────────────────────────────────────────────────
numpy>=1.26,<2.2
pandas>=2.1,<2.3
networkx>=3.2,<3.5
scikit-learn>=1.4,<1.6
xgboost>=2.0,<2.2
pyarrow>=15.0
python-dotenv>=1.0
PyYAML>=6.0
loguru>=0.7
tqdm>=4.66

# ─────────────────────────────────────────────────────────────
# AWS cloud connectivity (S3 data lake + Kinesis streaming)
# ─────────────────────────────────────────────────────────────
boto3>=1.34
botocore>=1.34

# ─────────────────────────────────────────────────────────────
# Optional / production-scale dependencies.
# The pipeline auto-detects these and falls back to lightweight
# pure-Python engines when they are not installed, so the demo
# runs out-of-the-box. Uncomment to enable the full stack.
# ─────────────────────────────────────────────────────────────
# pyspark>=3.5            # Structured Streaming engine (Databricks CE)
# graphframes>=0.6        # Distributed graph features
# catboost>=1.2           # Second model in the ensemble
# feast>=0.36             # Online/offline feature store
# redis>=5.0              # Hot feature cache (sub-ms lookups)
# mlflow>=2.10            # Model registry / experiment tracking
# langchain>=0.2          # Fraud investigation agent
# langchain-groq>=0.1     # Groq LLM backend for the agent
# evidently>=0.4          # Drift detection
# great-expectations>=0.18  # Data quality validation
